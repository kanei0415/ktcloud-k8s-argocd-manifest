apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: ktcloud-market-applications
  namespace: argocd
spec:
  generators:
    - list:
       elements:
        - app: ktcloud-msa-auth-service
          environment: release
          revision: HEAD
          ns: ktcloud-market-msa

        - app: ktcloud-msa-inventory-service
          environment: release
          revision: HEAD
          ns: ktcloud-market-msa

        - app: ktcloud-msa-order-service
          environment: release
          revision: HEAD
          ns: ktcloud-market-msa

        - app: ktcloud-msa-product-service
          environment: release
          revision: HEAD
          ns: ktcloud-market-msa

        - app: ktcloud-msa-user-api-gateway
          environment: release
          revision: HEAD
          ns: ktcloud-market-msa
  template:
    metadata:
      name: '{{ app }}-{{ environment }}'
    spec:
      project: default
      sources:
        - repoURL: https://github.com/ktcloud-msa/ktcloud-msa-chart.git
          targetRevision: '{{ revision }}'
          path: '.'
          ref: template
          helm:
            valueFiles:
              - '$values/{{ app }}/values-{{ environment }}.yaml'
        - repoURL: 'https://github.com/ktcloud-msa/ktcloud-msa-values.git'
          targetRevision: '{{ revision }}'
          ref: values
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{ ns }}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        managedNamespaceMetadata:
          labels:
            istio-injection: enabled
        syncOptions:
          - CreateNamespace=true
          - SkipDryRunOnMissingResource=true
          - ServerSideApply=true
      # ESO mutates ExternalSecret managedFields/status after creation,
      # leaving the resource permanently OutOfSync against the static chart
      # render even though the underlying Secret is SecretSynced.
      ignoreDifferences:
        - group: external-secrets.io
          kind: ExternalSecret
          jqPathExpressions:
            - '.status'
            - '.metadata.creationTimestamp'
            - '.metadata.generation'
            - '.metadata.resourceVersion'
            - '.metadata.uid'